# gbox 环境变量配置示例
# 复制此文件为 .env 或 .env.local 进行自定义配置
# 优先级: 命令行参数 > .env.local > .env > 默认值

# ============================================
# 资源限制配置
# ============================================

# 容器内存限制（默认: 4g）
# 示例: 4g, 8g, 16g
GBOX_MEMORY=4g

# 容器 CPU 核心数（默认: 2）
# 示例: 2, 4, 8
GBOX_CPU=2

# ============================================
# 网络配置
# ============================================

# 端口映射配置（默认: 不映射任何端口）
# 格式: "host_port:container_port" (分号分隔多个端口)
# 示例:
#   GBOX_PORTS="8000:8000"              # 宿主机 8000 -> 容器 8000
#   GBOX_PORTS="8080:8000"              # 宿主机 8080 -> 容器 8000
#   GBOX_PORTS="8000:8000;7000:7001"    # 多端口映射
# GBOX_PORTS=""

# Agent 网络代理（默认: 无）
# 示例: http://127.0.0.1:7890
# GBOX_PROXY=""

# ============================================
# 目录配置
# ============================================

# 只读参考目录列表（默认: 无）
# 格式: "目录路径" (分号分隔多个目录)
# 所有目录以只读方式挂载到容器中的相同路径
# 用于为 AI Agent 提供其他项目的代码参考
# 示例:
#   GBOX_REF_DIRS="/Users/me/project1"
#   GBOX_REF_DIRS="/Users/me/project1;/Users/me/project2"
# GBOX_REF_DIRS=""

# ============================================
# API 密钥配置
# ============================================

# Anthropic API Key（用于 Claude）
# ANTHROPIC_API_KEY=""

# ============================================
# 容器行为配置
# ============================================

# 退出后保留容器（默认: false）
# 设置为 true 后，退出 agent 时容器不会被删除，可以直接复用
# GBOX_KEEP=false

# 自定义容器名（默认: 自动生成）
# 格式: 字母、数字、下划线、点、连字符，必须以字母或数字开头
# GBOX_NAME=""

# 退出后自动清理容器（默认: false）
# 设置为 true 后，退出时自动删除容器
# GBOX_AUTO_CLEANUP=false

# ============================================
# 调试配置
# ============================================

# 调试模式（默认: 无）
# 设置为 happy:* 启用 Happy 的调试日志
# DEBUG=""

# gbox 调试模式（默认: 0）
# 设置为 1 启用 gbox 的调试输出
# GBOX_DEBUG=0

# ============================================
# 高级配置
# ============================================

# 容器就绪检查次数（默认: 30）
# GBOX_READY_ATTEMPTS=30

# 容器就绪检查间隔（秒，默认: 0.2）
# GBOX_READY_DELAY=0.2
